# 可疑错译待确认

以下条目来自 `item` 和 `menu` 目录的第二轮检查。表中内容尚未批量修复，建议确认标准译法后再统一处理。

| English / 原文 | 当前译法 | 出现位置 | 可疑点 | 建议确认 |
| --- | --- | --- | --- | --- |
| `Scion of the Sealed God` | `神嗣` / `受封印神祇之嗣` / `遭封印神祇的后嗣` | `item/GoodsName.fmg.txt`、`item/GoodsCaption.fmg.txt`、`item/NpcName.fmg.txt`、`item/PlaceName*.fmg.txt`、`menu/EventTextForTalk.fmg.txt`、`menu/TutorialBody.fmg.txt` | 同一个 BOSS / 追忆名存在多种译法，且 `后嗣` 与 `神嗣` 风格差异较大。 | 建议确认标准 BOSS 名。若保留短名，可统一为 `神嗣`；若强调设定，可统一为 `受封印神祇之嗣`。 |
| `Sealed God` | `被封印神祇` / `被封印之神` / `封印之神` | `item/GoodsCaption.fmg.txt`、`item/GoodsInfo.fmg.txt`、`item/ProtectorCaption*.fmg.txt`、`menu/GR_LineHelp.fmg.txt` | 神祇本体称呼不统一，会影响腐败侍仆、神嗣等相关文本。 | 建议确认一个主译名。当前较常见、较顺的是 `被封印之神`。 |
| `Keystone(s) of the Forge` | `熔炉钥石` / `大锅钥石` / `大锅的第…块钥石` / `圣钥石` | `item/GoodsName.fmg.txt`、`item/GoodsInfo.fmg.txt`、`item/GoodsCaption.fmg.txt`、`menu/TalkMsg.fmg.txt`、`menu/TutorialBody.fmg.txt` | `Forge` 在系统说明、物品名和剧情说明中分别译为 `熔炉`、`大锅`、`圣钥石`，概念边界不清。 | 建议确认系统总称与物品名是否分开。可选：系统说明用 `大锅钥石`，具体物品用 `大锅的第一块钥石` 等。 |
| `Fallingstar Beast` / `Full-Grown Fallingstar Beast` | `坠星兽物` / `坠星成兽` / `坠星兽` | `item/PlaceName*.fmg.txt`、`item/GoodsCaption.fmg.txt` | `坠星兽物` 和 `坠星成兽`偏官译，但说明文本中也出现 `坠星兽力量`。 | 建议确认是否沿用官译；若追求直观一致，可改为 `坠星兽` / `成年坠星兽`。 |
| `Capital Outskirts` | `王城外围` / `王城城墙外` / `王城外围城墙上` | `item/PlaceName_dlc02.fmg.txt`、`menu/EventTextForMap.fmg.txt` | 同一英文在地图区域和提示文本中译法不同，可能是同地点不同上下文，也可能是不一致。 | 建议结合游戏内地图标记确认。若同地点，建议统一为 `王城外围`。 |
| `Deathblight` / `deathblight buildup` | `咒死` / `死亡` / `死亡量表` / `死亡异常状态` | `item/GoodsCaption.fmg.txt`、`item/GoodsInfo.fmg.txt`、`menu/LoadingText.fmg.txt`、`menu/TutorialBody.fmg.txt` | 官译状态名偏 `死亡`，法术与武器说明多用 `咒死`，同一效果在说明中混用。 | 建议确认状态名和风味名是否分层：状态系统用 `死亡`，法术/设定名用 `咒死`。 |
| `Crystal Tear` / `Physick Tear` / `Physick Remnant` | `结晶露滴` / `灵药露滴` / `灵药遗物` | `item/GoodsName.fmg.txt`、`item/GoodsCaption.fmg.txt`、`menu/LoadingText.fmg.txt`、`menu/TutorialBody.fmg.txt` | 已修复一处将 `Physick Remnants` 漏译为 `结晶露滴` 的问题，但灵药相关道具仍有多个近义译名。 | 建议确认：`Crystal Tear` 固定为 `结晶露滴`，`Physick Remnant` 固定为 `灵药遗物`；`cracked physick tear` 是否保留 `破裂灵药露滴`。 |
| `Boss` | `Boss` / `BOSS` / `boss` | `menu/ActionButtonText.fmg.txt`、`menu/TutorialTitle.fmg.txt`、`menu/TalkMsg.fmg.txt`、`menu/TutorialBody.fmg.txt` | 大小写和中英文排版不统一。 | 建议统一为 `BOSS`，或统一为 `Boss`。当前 `术语对照表.md` 已记录 `BOSS复活圣坛`。 |
