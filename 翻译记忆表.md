# 翻译记忆表

本表记录已确认的翻译处理结果，以及本轮检查后决定保留现状的项目。后续翻译校对时，以 `术语对照表.md` 和本表共同作为参考。

| English / 原文 | 处理结果 |
| --- | --- |
| Boss / BOSS / boss | 保留英文；中文翻译行中仅将全小写 `boss` 改为 `Boss`，不加入术语表。 |
| Isolated Merchant | 统一为 `隐居商人`。 |
| Cathedral of Dragon Communion | 统一为 `大龙飨教堂`。 |
| Scadutree Avatar | 统一为 `幽影树的化身`。 |
| Onyx Lord | 统一为 `石肤黑王`。 |
| Margit, the Fell Omen | 统一为 `“恶兆妖鬼”玛尔基特`。 |
| Bloodflame Scythe | 同名不同义，保留现状：法术名为 `血焰之镰`，武器语境为 `血焰大镰刀`，不加入术语表。 |
| Bloodflame Dragon Sanguivaros | 统一为 `“血焰龙”桑吉瓦罗斯`，并加入术语表。 |
| Bayle the Dread | 统一为 `“狂龙”贝勒`。 |
| Keystone(s) of the Forge | `Forge` 在该术语中统一为 `大锅`；系统总称为 `大锅钥石`，具体物品名沿用 `大锅的第…块钥石`。 |
| Sealed God | `封印之神`、`被封印之神`、`被封印神祇` 等本体称呼修正为 `受封印神祇`，并加入术语表。注意正确字形为「神祇」（礻+氏），非「神祗」。 |
| Scion of the Sealed God | 保留现有译法，不再统一；仅修正错误使用 `封印之神` 指代 `Sealed God` 本体的文本。 |
| twisted divine element / twisted deity | 涉及萝蜜娜、花蕾圣女追忆、外在神祇传说护符等语境时，保留 `异样的为神本质`；不要润色或泛化为 `异样的神性本质`。这是贴合原文和既有官译的设定表达。 |
| The Lord accepted the sap stoically, without any sign of wavering. | 保留既有译法 `据说在受赐的当下，他文风不动。`；不要改成 `他没有一丝动摇` 这类顺滑但偏离既有文本风格的表述。 |
| Fallingstar Beast / Full-Grown Fallingstar Beast | 按确认原则保留现状，不作为当前待处理项。 |
| Capital Outskirts | 按确认原则保留现状，不作为当前待处理项。 |
| Deathblight / deathblight buildup | 按确认原则保留现状，不作为当前待处理项。 |
| Crystal Tear / Physick Tear / Physick Remnant | 按确认原则保留现状，不作为当前待处理项。 |
| Dream (sleep spells) | 统一为 `梦境`（非 `噩梦`），已加入术语表。 |
| Vapor | 统一为 `水雾`（非 `灵汽`），已加入术语表。 |
| Gauntlets / Bracers / Wraps / Hood / Hat (armor) | 统一为 `臂甲` / `护腕` / `腕带` / `风帽` / `帽子`，已加入术语表。Legwraps 保留官方特殊翻译（`绑腿` / `裹腿`），不统一。 |
| Faint / Shimmering / Glowing / Shining / Radiant / Shadow (Rune) | 统一为 `微光` / `闪光` / `明亮` / `闪耀` / `辉光` / `幽影`（卢恩前缀），禁止使用 `初光` / `亮光` / `盛光` / `影光` / `微重` 等非标前缀。已加入术语表。 |
| Magma Rune | 统一为 `岩浆卢恩`（非 `熔岩卢恩`），与 GoodsName 道具名称保持一致。 |
| Frost Rune | 统一为 `冰霜卢恩`（非 `寒冰卢恩`），与 GoodsName 道具名称保持一致。 |
| Spell Rune | 译为 `法术卢恩`（用于获取法术的一种卢恩），不要译作 `卢恩法术` 或 `符文法术`。 |
| Runebear | 统一为 `卢恩熊`（与游戏本体官译一致），不要使用 `符文熊`。 |
| Page (armor) | 防具套装译为 `随从手套` / `随从长裤` / `随从上衣` / `随从风帽`；High Page = `上流随从`。不要使用 `侍从` / `高级侍从` 系列。 |
| Ragged (armor) | 防具套装译为 `拼接手套` / `拼接腰巾` / `拼接铠甲` / `拼接帽子`，不要使用 `破布` 系列。 |
| Ordina, Liturgical Town | 官方译名为 `"仪典镇"奥缇那`，不要写成 `奥缇娜`。 |
| Caelem Ruins | 官方译名为 `奇列姆废墟`，不要使用 `凯利姆废墟` / `凯列姆废墟`。 |
| Chelona's Rise | 官方译名为 `希耶罗那魔法师塔`（注意与 Caelem 完全不同），不要写成 `奇列姆的塔`。 |
| Belurat / "Tower Settlement" | 统一为 `贝瑞特` / `"塔之镇"贝瑞特`，不要使用 `贝鲁拉特`。 |
| Bonny Gaol | 官方译名为 `波尼监牢`，不要写成 `邦妮监牢`。 |
| Shaded Castle | 官方译名为 `日荫城`，不要使用 `荫蔽城堡`。 |
| Unsightly Catacombs | 官方译名为 `丑恶地下墓地`，不要使用 `丑陋地下墓地`。 |
| Road's End Catacombs | 官方译名为 `绝路地下墓地`，不要使用 `路尽地下墓地`。 |
| Caelid Waypoint Ruins | 官方译名为 `盖利德的驿站街遗迹`，不要使用 `盖利德路标遗迹`。 |
| Sainted Hero's Grave | 官方译名为 `尊贵者的英雄墓地`，不要使用 `圣徒英雄墓地`。 |
| Auriza Hero's Grave / Auriza Side Tomb | 官方译名为 `亚雷萨英雄墓地` / `亚雷萨副墓地`，不要使用 `奥利萨` 系列。 |
| Tombsward Cave | 官方译名为 `灵庙原野洞窟`，不要使用 `守墓洞穴`。 |
| Tombsward Ruins | 官方译名为 `灵庙原野的废墟`，不要使用 `墓园废墟`。 |
| Perfumer's Grotto | 官方译名为 `调香师的隐藏洞窟`，不要使用 `调香师的石窟`。 |
| Tragoth | 统一为 `忒拉格斯`（不要漏写最后的 `斯`）。 |
| Maleigh Marais 等使用 `玛莉卡` 一词 | 官方译名为 `玛莉卡`，不要写成 `玛丽卡`。 |
| Depraved Perfumer (armor) | 官方译名为 `堕落调香师`，不要使用 `堕恶调香师`。 |
| Velvet Sword of St. Trina (Convergence MOD 武器) | 暂译为 `天鹅绒托莉娜剑`（参考游戏原文表 `velvet`=`天鹅绒`，St. Trina 译作 `托莉娜` / `圣托莉娜`）。 |
| GoodsCaption.fmg.txt 中 8801/8822/8891/8892/8894 多条 = 行存在 UTF-8 替换字符 (U+FFFD) | 已对照 C 行与英文版恢复：`紫 bloody�指` → `纯紫血指`、`血�的指` → `血指`、`鲜�贵族` → `鲜血贵族`、`米德拉的�剑` → `米德拉的大剑`、`托莉娜的鹅�剑` → `天鹅绒托莉娜剑`。 |
| 6312 条 (Sanguine Nobles 的祷告) 中的 `燃血的符文` | 是对英文 `fonts of burning blood` 的错译，改为 `燃血之泉，亦可由鲜血贵族的华美匕首引出`。注意 `font`(泉/源) ≠ `rune`(卢恩)。 |
